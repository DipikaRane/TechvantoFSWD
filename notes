using GitBash
// only first time for windows
1. go inside the folder
2. right click
3. click Git Bash Here
4. write git clone (copy repo link) and enter

//next all the time for windows
1. go inside the folder
2. right click
3. click Git Bash Here
4. write git pull and enter

ways to write css
1. Inline styles
2. Internal
3. External

CSS styling
background
color
flex
grid
inline-block
z-index -1 to place behind, 100 to place in front

HTML and CSS project
1. portfolio page
2. Registration form

topics to be covered:
animation
hover
box-shadow

##### BOOTSTRAP ########
Grid Basics
Bootsrtap 3
xs -extra small --- for phone/mobile -screen < 768px wide
sm - small ---- for tablets - screen > 768px wide
md -medium ---- for small laptops screen = > 992px
lg - large --- for laptops and desktop devices = > 1200px wide

Bootstrap 4
col-xs < 576px
col-sm > 576px
col-md > 768px
col-lg > 992px
col-xl >1200px

Bootsrtap 5
col-xs < 576px
col-sm > 576px
col-md > 768px
col-lg > 992px
col-xl > 1200px
col-xxl > 1400px

create emplyee.json file 

###form method################################
1. GET -- default method
2. POST -- to send/save method


to create dummy url

#. Install nodejs (https://nodejs.org/en/)
1. go to browser
2. open link

#for windows (one time only)
1. open command prompt as administrator
2. npm i -g json-server
3. close cmd

next step each time 
1. open terminal in vs-code
2. json-server --watch jsonFileName.json --port 8500

file:///G:/Techvanto/FSWD/Bootstrap/date21april22.html?eid=A982ST&fname=Nitin&lname=Sharma&email=nitins.rane%40gmail.com&password=nit&search=&date=2022-04-20&gender=male#


bootstrap 3 uses glyphicon, img-responsive
bootstrap 4 favicon, img-fluid, carousel, colors, btn-size